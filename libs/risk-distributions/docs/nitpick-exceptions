# vivarium.risk_distributions
# Type aliases don't play nicely with sphinx when you import them
# elsewhere.  Works fine for static type checker though.  I think this
# is cause sphinx does runtime checks.
py:class vivarium.risk_distributions.formatting.Parameter
py:class vivarium.risk_distributions.formatting.Parameters

# Need these for python 3.6
py:class Parameter
py:class Parameters

# Numeric container type aliases (see risk_distributions.py). Same story as
# Parameter/Parameters above: sphinx tries to resolve the alias name as a class.
py:class Numeric
py:class NumericInput

# pandas
py:class pandas.core.frame.DataFrame
py:class pandas.core.series.Series
py:class pandas.core.indexes.base.Index

# Type hints use the abbreviated ``pd.``/``npt.`` aliases. Because of
# ``from __future__ import annotations`` these stay as strings, so
# ``autodoc_typehints = "description"`` renders them verbatim and sphinx
# cannot resolve the alias to a documented class.
py:class pd.DataFrame
py:class pd.Series
py:class pd.Index
py:class npt.NDArray
