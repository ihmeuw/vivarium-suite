**1.0.0 - 05/20/26**

Initial release as a standalone package extracted from ``vivarium``. This provices
the data-artifact model (``Artifact``, ``ArtifactException``) and the HDF5 storage
backend (``EntityKey`` plus the read/write/load/remove functions).
